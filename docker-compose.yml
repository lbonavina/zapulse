services:
  whatsapp-api:
    build: .
    container_name: whatsapp-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      # Persiste a sessão do WhatsApp fora do container
      - ./auth:/app/auth
    environment:
      - PORT=3000
      - NODE_ENV=production
